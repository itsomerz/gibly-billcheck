// Shared design tokens for the gibly "found money" look — plum canvas, sunshine
// gold, money-positive mint. Used by both BillCheck and the /rates page so the
// palette only lives in one place.
export const P = {
  plum:    "#2A1A3E",   // deep aubergine canvas
  plum2:   "#382450",   // raised surface
  plum3:   "#462D63",   // hover / accent surface
  line:    "#4A3866",
  gold:    "#FFC94D",   // sunshine gold — the star
  goldDim: "#E0A82E",
  mint:    "#5FE3B0",   // money-positive
  mintDim: "#3FBF90",
  coral:   "#FF7A6B",   // gentle warning
  cream:   "#F6EEFB",   // primary text
  lilac:   "#B9A6D0",   // muted text
};

export const F = {
  display: '"Fraunces", Georgia, serif',
  body: '"Plus Jakarta Sans", system-ui, sans-serif',
};
